<tr>
  <td><%= link_to image_tag(collection.image.url, size: "32", alt: collection.name), collection %></td>
  <td><%= link_to collection.id, collection %></td>
  <td><%= link_to collection.name, collection %></td>
  <td><%= collection.recipes.size %></td>
  <td><%= "✔" if collection.featured == true %></td>
  <td><%= collection.updated_at.strftime("%m-%d-%Y") %></td>
  <td><%= link_to "Edit", edit_collection_path(collection), class: "btn btn-primary btn-sm", role: "button" %></td>
  <td><%= link_to "Delete", collection, method: :delete, class: "btn btn-danger btn-sm", role: "button", data: { confirm: "Are you sure you want to delete this collection?" } %></td>
</tr>